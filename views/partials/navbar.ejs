<nav>
    <a href="/" class="navHomeButton">Home</a>
    <% if(locals.user) { %>
        <a href="/secret">Secret</a>
    <% } %>
    <div class="navControls">
        <% if(locals.user) { %>
            <a href="/logout">Log out</a>
        <% } else { %>
            <a href="/signup">Sign Up</a>
            <a href="/login">Log In</a>
        <% } %>
    </div class="navControls">
</nav>